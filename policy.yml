policies:
  - name: terminate-on-create
    resource: ec2
    mode:
      type: cloudtrail
      events:
       - RunInstances
    actions:
      - type: terminate
  - name: terminate-pull
    resource: ec2
    mode:
      type: pull
    actions:
      - type: terminate
